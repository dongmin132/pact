{
  "name": "pact",
  "version": "0.2.0",
  "description": "pact — 계약 기반 AI 개발 운영 플러그인 (Claude Code)",
  "private": true,
  "bin": {
    "pact": "./bin/pact"
  },
  "scripts": {
    "test": "node --test test/"
  }
}
